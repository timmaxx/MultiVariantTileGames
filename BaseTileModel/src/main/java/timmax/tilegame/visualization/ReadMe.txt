Пакет visualisation может (и должен) зависеть от классов и пакетов содержащих логику.
Ни один из классов с логикой не должен зависеть от классов в пакете visualisation!

В дальнейшем можно вынести этот пакет в отдельный модуль, и даже больше: визуализацию можно вынести и в отдельный сервер.
