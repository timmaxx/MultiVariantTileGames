package timmax.tilegame.basemodel.protocol;

public enum TypeOfTransportPackage {
    LOGIN,
    LOGOUT/*,
    GAME_TYPE_MAP,
    SELECT_GAME_TYPE,
    CREATE_GAME_SERIES,
    GAME_SERIES_MAP,
    SELECT_GAME_SERIES,
    GAME_MATCH_MAP,
    SELECT_GAME_MATCH,
    PLAYER_SIDE_MAP,
    SELECT_PLAYER_SIDE,
    DECLARE_READINESS,
    READINESS_MAP
*/
}