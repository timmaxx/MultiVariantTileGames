package timmax.tilegame.websocket.client;

import timmax.tilegame.basemodel.ServerBaseModel;

public interface Observer033OnSelectGameType {
    void updateOnSelectGameType(Class<? extends ServerBaseModel> serverBaseModelClass);
}